import 'package:flutter/material.dart';

class PendonorCard extends StatelessWidget {
  const PendonorCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
